breach-probe.html
breach-sv-*.html
probe-out/

# AAA review loop capture output — frames are evidence for a round, not source
shots/
# ...but docs/frames/ IS tracked: it is the committed reference pair the A/B depends on, and a
# capture that lives only in the working tree does not survive a container rollback.
!docs/frames/
