# RUMPUS ENGINE — the community library is readable cross-origin (build 958):
# the GitHub Pages copy of the game fetches this same catalog, so both hosts share one library.
# (GitHub Pages ignores .htaccess; this takes effect on the cPanel host.)
<IfModule mod_headers.c>
  Header set Access-Control-Allow-Origin "*"
</IfModule>
